NODE_ENV=production

PORT=5000

# MongoDB runs inside Docker
MONGO_URI=mongodb://mongo:27017/lifedrop

# JWT secret (must come from GitHub Secrets in CI/CD)
JWT_SECRET=your_super_secure_jwt_secret_here

